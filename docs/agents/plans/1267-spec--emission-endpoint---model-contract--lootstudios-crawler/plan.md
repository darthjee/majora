# Plan: Spec — Emission endpoint & model contract (Lootstudios crawler)

Issue: [1267-spec--emission-endpoint---model-contract--lootstudios-crawler.md](../../issues/1267-spec--emission-endpoint---model-contract--lootstudios-crawler.md)

## Overview

Documentation-only spec sub-issue: write
`docs/agents/specs/loot-crawling/emission-endpoint.md`, mapping the finalized
`GetMyLootsCache` field extractions from the two sibling sub-issues onto the
exact request body #1262's `stl_models/import.json` endpoint expects, plus a
Navi `body_template` sketch. No code changes.

See [crawler.md](crawler.md) for the full plan.
