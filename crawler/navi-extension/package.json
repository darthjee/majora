{
  "name": "navi-loot-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Navi extension (backend route + frontend page + menu entry) for the Majora Lootstudios crawler.",
  "scripts": {
    "build": "vite build && mkdir -p dist/backend && cp src/backend/*.js dist/backend/",
    "test": "docker compose run --rm extension_tests"
  },
  "author": "darthjee",
  "license": "MIT",
  "dependencies": {
    "navi-hey-client": "^0.2.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "7.14.2",
    "vite": "^7.2.4"
  }
}
