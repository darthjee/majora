"""Statistics app configuration."""

from django.apps import AppConfig


class StatisticsConfig(AppConfig):
    """Configuration for the statistics app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'statistics'
