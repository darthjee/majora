# NPC character-treasure endpoint rules. No owner concept. `create` (used by the treasure
# summary's `/all.json` variant, issue #1001) mirrors `game_npc_item`'s own `restricted.create`
# tier (staff only).
restricted:
  create:
    - staff
