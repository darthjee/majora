"""Miniatures app configuration."""

from django.apps import AppConfig


class MiniaturesConfig(AppConfig):
    """Configuration for the miniatures app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'miniatures'
