"""Domains app configuration."""

from django.apps import AppConfig


class DomainsConfig(AppConfig):
    """Configuration for the domains app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'domains'
