import CharacterPossession from './shared/CharacterPossession.jsx';

/**
 * NPC possession detail page (issue #1076).
 *
 * @returns {React.ReactElement} NPC possession detail page element.
 */
export default function NpcCharacterPossession() {
  return <CharacterPossession characterKind="npcs" />;
}
