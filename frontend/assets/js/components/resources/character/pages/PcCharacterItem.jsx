import CharacterItem from './shared/CharacterItem.jsx';

/**
 * PC item detail page (issue #724).
 *
 * @returns {React.ReactElement} PC item detail page element.
 */
export default function PcCharacterItem() {
  return <CharacterItem characterKind="pcs" />;
}
