import CharacterItemNew from './shared/CharacterItemNew.jsx';

/**
 * NPC item creation page.
 *
 * @returns {React.ReactElement} NPC item creation page element.
 */
export default function NpcCharacterItemNew() {
  return <CharacterItemNew characterKind="npcs" />;
}
