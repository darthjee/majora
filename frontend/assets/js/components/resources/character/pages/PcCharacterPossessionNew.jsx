import CharacterPossessionNew from './shared/CharacterPossessionNew.jsx';

/**
 * PC possession creation page.
 *
 * @returns {React.ReactElement} PC possession creation page element.
 */
export default function PcCharacterPossessionNew() {
  return <CharacterPossessionNew characterKind="pcs" />;
}
