import CharacterDocument from './shared/CharacterDocument.jsx';

/**
 * PC document detail page (issue #892).
 *
 * @returns {React.ReactElement} PC document detail page element.
 */
export default function PcCharacterDocument() {
  return <CharacterDocument characterKind="pcs" />;
}
