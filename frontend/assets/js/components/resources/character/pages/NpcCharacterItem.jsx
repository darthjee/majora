import CharacterItem from './shared/CharacterItem.jsx';

/**
 * NPC item detail page (issue #724).
 *
 * @returns {React.ReactElement} NPC item detail page element.
 */
export default function NpcCharacterItem() {
  return <CharacterItem characterKind="npcs" />;
}
