import CharacterPossession from './shared/CharacterPossession.jsx';

/**
 * PC possession detail page (issue #1076).
 *
 * @returns {React.ReactElement} PC possession detail page element.
 */
export default function PcCharacterPossession() {
  return <CharacterPossession characterKind="pcs" />;
}
