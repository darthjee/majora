import CharacterPossessionNew from './shared/CharacterPossessionNew.jsx';

/**
 * NPC possession creation page.
 *
 * @returns {React.ReactElement} NPC possession creation page element.
 */
export default function NpcCharacterPossessionNew() {
  return <CharacterPossessionNew characterKind="npcs" />;
}
