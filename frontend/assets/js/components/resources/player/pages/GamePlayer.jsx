import PlayerDetail from './shared/PlayerDetail.jsx';

/**
 * Player detail page (issue #695).
 *
 * @returns {React.ReactElement} Player detail page element.
 */
export default function GamePlayer() {
  return <PlayerDetail />;
}
